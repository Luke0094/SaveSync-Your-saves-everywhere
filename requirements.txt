# pip install -r requirements.txt
PySide6>=6.6.0
psutil>=5.9.0
watchdog>=3.0.0
requests>=2.31.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.100.0
msal>=1.24.0
dropbox>=11.36.0
webdavclient3>=3.14.6
python-dateutil>=2.8.2
pynput>=1.7.6
cryptography>=41.0.0
keyring>=24.0.0
Pillow>=10.0.0
pillow-avif-plugin>=1.0
# core.engines.wolf_lz4 (Wolf RPG Editor's LZ4-compressed save variant):
# lz4 decompresses/recompresses the payload, numpy vectorises the seed
# search that unlocks it, numba JIT-compiles that same search roughly
# 3-4x faster, measured — see that module's own docstring for why a
# search, not a formula. numba's own import stays try/except in code (a
# source checkout without it still falls back to the numpy path), but is
# listed here as a real dependency so a normal install/build gets the
# faster path by default.
lz4>=4.0.0
numpy>=1.24.0
numba>=0.60.0

# ── Test / tools ──────────────────────────────────────────────────────────
langdetect>=1.0.9
PyYAML>=6.0

# ── Build ──────────────────────────────────────────────────────────────────
# pip install pyinstaller
#
# Build EXE (from project root):
#   pyinstaller savesync.spec
#
# Output: dist/SaveSync/SaveSync.exe
